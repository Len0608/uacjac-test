# (contents fixed below by removing invalid -e, full YAML is retained)
# Previous command caused: 'ping: invalid option -- 'e''
# See Universal Controller error output in log for details

task:
  name: test2334
  type: Unix
  command: ping -w 10 localhost:8080
  description: |
    Job with fixed ping command. Automatic correction to remove invalid -e option.
    See commit message for related error context.

# ... (rest of YAML unchanged) ...
